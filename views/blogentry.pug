extends index.pug

block content
  section.layout-width-sd.layout-col
    .layout-width-1-1.layout-col
      .layout-width-1-1.layout-col
        h1 #{note.name}
        h3 #{note.user.fullname}
        p #{note.updatedAt}
      .layout-width-1-1.layout-col
        p #{note.description}
      .hidden.markdownInput #{note.body}
      .layout-width-1-1.layout-col
        div#markdownOutput.layout-col
  script(src="https://cdnjs.cloudflare.com/ajax/libs/showdown/1.8.6/showdown.min.js")


extends index.pug

block content
  .layout-col
    .section-hdg.section-hdg-1 Create Note
    .user-listing.layout-col
      form(action="/notes/" method="post")
        input(id="user" type="hidden" name="user" value=user.id)
        input(id="domain" type="hidden" name="domain" value=domain.id)
        .inputnotename
          input#name(type="text" name="name" placeholder="Name or Title" autofocus)
        .inputnotedescription
          input#description(type="text" name="description" placeholder="Description of the note")
        .inputnotebody
          input#body(type="text" name="body" placeholder="Note text")
        .submit.itemdetail.layout-col
          .label Actions:
          .data
            input#submit.btn.normal.buttonconfirm(type="submit" value="Confirm")
            a.btn.cancel.buttoncancel(href="/notes/") Cancel

footer
  nav(role=navigation)
    div.navleft
      .copyright &copy;2019 Travis Colbert
      .madewith Made with Laminar
    div.navcenter
      div 
        | Hi 
        if(account)
          | &nbsp;#[a(href='/profile/') #{account}]
    div.navlinks
      ul
        li
          a(href="mailto:travis@traviscolbert.net")
            i.material-icons.menu-icon mail_outline
        //- each item in menu
        //-   - var link = item.link;
        //-   - var text = item.text;
        //-   li
        //-     a(href=link)
        //-       i.material-icons.menu-icon #{item.icon}
        //-       .nobreak #{text}

script(src='/js/authUI.min.js')
script(src='/js/onboard.min.js')

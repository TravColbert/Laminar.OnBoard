extends index.pug

block aside
  aside.sidebar
    if(user)
      article#actions
        .hdg.hdg-1 Actions
        #note_add_button_container.addnote.authUI(data-fetch="note_add" data-href="/actions/create/notes/")
        #note_edit_button_container.editnote.authUI(data-fetch="note_edit" data-href="/notes/" + note.id + "/actions/edit/")
      +list-user-invites()
      article#articles
        include articles-list
      +list-user-domains("Domain")
      +create-or-see-domains("Domain")
    if(!user)
      if(notes && notes.length>0)
        article#articles
          include articles-list
      article#getstarted
        //- .hdg.hdg-2 Get Started!
        +login-or-register("Login with your account to begin using your cool app!","Register with your email address to create an account.")

block content
  section.note
    article.main
      .hdg.hdg-1 #{note.name}
      .item-listing
        .itemdetail.articleauthor
          .data #{note.user.firstname} #{note.user.lastname}
        .itemdetail.articledate
          .data #{note.updatedAt}
        .itemdetail.articledomain
          .data #{note.domain.name}
        .itemdetail.articledescription
          .data #{note.description}
        if(user)
          .itemdetail.articlevisibility
            label.label Public:
            .data
              input#public.hidden(type="checkbox" name="public" value="true" disabled checked=!!(note.public))
              label.togglepublicfalse(for="public")
                i.material-icons.toggle.toggleoff radio-button-unchecked
              label.togglepublictrue(for="public")
                i.material-icons.toggle.toggleon radio_button_checked
        .itemdetail
          .notebody !{note.html}
    //- article.secondary
    //-   .hdg.hdg-1 Note Actions
    //-   #note_edit_button_container.editnote.authUI(data-fetch="note_edit" data-href="/notes/" + note.id + "/actions/edit/")
    //-   .description
    //-     .infotext.hdg.hdg-3 Note View
    //-     .infotext This is a list of users. You may edit the user's details by clicking on a user entry.
    //-     .infotext Click the "Add User" button to create a new user.
    //-     .infotext Each user is given a user ID that is used by stockr to keep track of the user and the user's cases.
